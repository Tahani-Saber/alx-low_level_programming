#include "main.h"

/**
 * print_rev - check the code
 * @s: the function parameter
 *
 * Return: return void
*/

void print_rev(char *s)
{
	int count, i;

	while (str[i] != '\0')
	{
		i++;
	}
	for (count = i - 1; count >= i; i--)
	{
		putchar(str[i]);
	}
	putchar('\n');
}
