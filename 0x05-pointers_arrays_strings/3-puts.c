#include "main.h"

/**
 * _puts - check the code
 * @str: string to be printed
 *
 * Return: return void
*/

void _puts(char *str)
{
	int a = 0;

	while (str[a] != '\0')
	{
		putchar(str[i]);
		i++;
	}
	putchar('\n');
}
