#include "main.h"

/**
 * _strlen - clculate the string length
 * @s: describe the string length
 *
 * Return: 0
*/

int _strlen(char *s)
{
	char a[1000];
	int i;

	while (a[i] != '\0')
	{
		i++;
	}
	*s = i;
	return (*s);
}
